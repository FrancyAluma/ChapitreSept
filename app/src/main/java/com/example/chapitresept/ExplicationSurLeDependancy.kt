package com.example.chapitresept

/*



[versions]
# just version numbers go here, e.g.:
compose-navigation = "2.7.6"

[libraries]
# full coordinates go here, using the version above if you want:
compose-navigation = { module = "androidx.navigation:navigation-compose", version.ref = "compose-navigation" }

# OR, if you don’t want to use a version reference:
# compose-navigation = "androidx.navigation:navigation-compose:2.7.6"







 */